services:
  backend:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run docker:dev
    environment:
      - CHOKIDAR_USEPOLLING=true
